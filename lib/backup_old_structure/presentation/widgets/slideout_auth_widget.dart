import 'package:flutter/material.dart';

class SlideoutAuthWidget extends StatelessWidget {
  const SlideoutAuthWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // Replace with your widget tree
  }
}
