{
  "name": "multisales-functions",
  "version": "1.0.0",
  "description": "Cloud Functions pour le projet MULTISALES (multisales-18e57)",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint . || true",
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project=multisales-18e57"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0",
    "googleapis": "^139.0.0",
    "@sendgrid/mail": "^7.7.0"
  },
  "devDependencies": {
    "eslint": "^8.50.0"
  }
}
